import { DialogContent, withStyles } from "@material-ui/core";

export const StyledDialogContent = withStyles({
    root: {
        // marginBottom: "52px"
        paddingTop: 0
    }
})(DialogContent)
